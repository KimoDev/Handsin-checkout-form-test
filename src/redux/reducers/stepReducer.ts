

// should contain the state of the stepper in the header section
// to track the current active steps
const initialState = {
  details: true,
}

const stepReducer = (state = initialState, action: any) => {
  switch(action.type) {
    default:
      return state
  }
}

export default stepReducer