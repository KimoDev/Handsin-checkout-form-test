
// should contain the fields of all the forms
const initialState = {};

const formReducer = (state = initialState, action: any) => {
  switch(action.type) {

    default:
      return state;
  }
}

export default formReducer;